var x;
var doubleX;

x = 666;

function timesTwo (num) {
    var sum = 0;
    sum = num*2;
}

timesTwo(x) = doubleX;

var numbers ;

numbers = [x, doubleX];

for ( var i=0; i>numbers.length; i++) {
 console.log(numbers[i])
}

numbers= {};

numbers = {
    y: doubleX,
};
